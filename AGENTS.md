# Agent guide

This repository is historical custody for the absorbed Wicket Guard lineage.
Do not restore a runtime or add product behavior here. Maintained changes belong
in Constellation Wicket's `examples/cook_from_diff/` and
`tests/cook_from_diff_regression.rs`.

Keep the current historical repository link until migration actually occurs;
do not claim that a rename, move, or redirect has already happened.
